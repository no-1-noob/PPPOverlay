server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Cache Angular static assets aggressively
    location /assets/ {
        try_files $uri =404;

        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Never aggressively cache the application shell
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # Angular SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}